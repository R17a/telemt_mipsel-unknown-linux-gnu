# Cross.toml — конфигурация для MIPS (Keenetic Viva)
# Документация: https://github.com/cross-rs/cross

[target.mipsel-unknown-linux-gnu]

# Собираем std из исходников (обязательно для Tier 3)
build-std = true

# 🔧 ИСПРАВЛЕНО: image — простая строка
image = "ghcr.io/cross-rs/mipsel-unknown-linux-gnu:main"

# Установка зависимостей в Docker-контейнере
pre-build = [
    "apt-get update && apt-get install -y pkg-config libssl-dev musl-tools"
]

# 🔧 ИСПРАВЛЕНО: УБРАЛИ -C panic=abort (вызывает ошибку с panic_abort crate)
[target.mipsel-unknown-linux-gnu.env]
passthrough = [
    "RUSTFLAGS=-C linker-plugin-lto -C opt-level=z -C strip=symbols"
]
